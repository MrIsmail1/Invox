{% extends 'base.html.twig' %}

{% set title = "Invox | Confirmation réussie" %}
{% block body %}
    <body>
    <main class="relative" x-data="{ countdown: 7 }" x-init="() => {
      setInterval(() => {
      if (countdown > 0) {
      countdown--;
      } else {
      window.location.href = '/login';
      }
      }, 1000);
      }">
        <div class="absolute z-10 inset-3 w-1/5 transition-opacity ease-in duration-1000 opacity-100">
            <twig:Alert headerMessage="Redirection automatique"
                        xText="'Redirection en ' + countdown +'s vers la page de connexion. En cas de problème, veuillez cliquer sur le button ci-dessous.'"
                        action="Connexion"
                        actionRoute="{{ path('app_login') }}"
            />
        </div>
        <div class="w-full mx-auto flex h-screen">
            <div class="flex w-full h-full flex-col items-center justify-center bg-white-font relative">
                <div class="flex flex-col justify-center rounded-lg bg-bg-button w-[70%] shadow-xl">
                    <div class="flex h-fit items-center justify-center p-3">
                        <img
                                src="{{ asset('build/images/logoAuthentication.png') }}"
                                alt="invox-logo"
                                class="w-[30rem]"
                        />
                    </div>
                    <hr/>
                    <div class="flex h-full flex-col justify-center gap-7 px-28 py-12">
                        <p class="text-bg-black font-medium text-lg">
                            Votre confirmation a été un succès. Vous pouvez maintenant accéder à votre compte.
                        <p class="text-bg-black font-medium text-lg">
                            Nous vous remercions pour votre confiance ! Si vous avez des questions, n'hésitez pas
                            à <a href="#" class="text-pr-blue underline decoration-pr-grey">nous contacter.</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </main>
    </body>
{% endblock body %}