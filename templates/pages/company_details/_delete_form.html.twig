<form method="post" action="{{ path('app_company_details_delete', {'id': company_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_detail.id) }}">
    <button class="btn">Delete</button>
</form>
